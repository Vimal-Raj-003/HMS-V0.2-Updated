/** The same transport every other feature uses, for the same reason. */
export {
  newIdempotencyKey,
  queryString,
  request,
  type ClinicalRequest as ProcedureRequest,
} from '@/features/clinical/api/http';
