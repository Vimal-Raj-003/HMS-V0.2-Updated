{
  "name": "@vims/i18n",
  "version": "0.1.0",
  "private": true,
  "description": "next-intl message catalogues. en-IN default plus hi, ta, te, ml, kn, mr, bn, gu, or, pa and ar (RTL) \u2014 the locale superset fixed by DECISIONS D-13.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./messages/*": "./src/messages/*.json"
  },
  "scripts": {
    "build": "if [ -d src ]; then tsc --noEmit; else echo \"build: no source yet \u2014 added by the Phase 0 step that builds this package\"; fi",
    "lint": "eslint src --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "if [ -d src ]; then tsc --noEmit; else echo \"typecheck: no source yet \u2014 added by the Phase 0 step that builds this package\"; fi",
    "test": "vitest run --passWithNoTests",
    "coverage:check": "tsx src/check-coverage.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "vitest": "^4.1.10"
  }
}
